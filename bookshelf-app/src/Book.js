class Book {
    constructor(title, author, isbn, imageUrl) {
      this.title = title;
      this.author = author;
      this.isbn = isbn;
      this.imageUrl = imageUrl;
    }
  }
  
  export default Book;